import type { Request, Response } from "express";

export const hanldeShortOrders = (req: Request, res: Response, userId: string, stockSymbol: string, type: string, side: string, price: number, quantity: number) => {
	try {
		
	} catch (error) {
		console.log(error)
	}
}